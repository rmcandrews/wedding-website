import React from "react";
import ReactModal from "react-modal";

const RSVP = ({ isOpen }) => {
  return (
    <ReactModal isOpen={isOpen} className="modal">
      SOON TM
    </ReactModal>
  );
};

export default RSVP;
